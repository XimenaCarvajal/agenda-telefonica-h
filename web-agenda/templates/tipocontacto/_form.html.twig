{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="button" class="btn btn-outline-info">{{ button_label|default('Save') }}</button>
    <a href="{{ path('tipocontacto_index') }}"><button type="button" class="btn btn-outline-danger">Cancel</button></a>
{{ form_end(form) }}
